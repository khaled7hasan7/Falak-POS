# البرومت الثاني لـ Claude Code — إكمال المرحلة M1 (المهام 1.2 → 1.8)

> يُلصق كما هو في Claude Code من داخل مجلد المشروع. الأساس (1.1 و1.9) منجز ومقبول؛ هذا البرومت يكمل بقية M1.

---

اقرأ `CLAUDE.md` كاملاً — وخاصة **القسم 7 "قواعد كل جلسة"** فهو ملزم في كل ما يلي — ثم افتح من `docs/`: الملف 02 (قاعدة البيانات)، الملف 03 (نظام التصميم)، الملف 01 §5–§6 (الأدوار والصلاحيات)، والملف 04 §5/M1 (المهام). لا تكتب كوداً قبل ذلك.

**المهمة:** إكمال المرحلة M1 بتنفيذ المهام 1.2 حتى 1.8 من `docs/04-execution-plan.md`، وإصلاح عيبين موجودين الآن.

**اعرض أولاً خطة قصيرة** (ترتيب التنفيذ، أي subagents ستشغّل بالتوازي، أي اختبارات ستُكتب) وانتظر موافقتي، ثم نفّذ على فرع `feat/m1-core`.

## 0. إصلاحان قبل أي شيء (main مكسور حالياً)

1. `pnpm db:migrate` في CI يفشل لأن `@falak/db` غير موجودة — ستُحل بإنشاء الحزمة في الخطوة 1.
2. `pnpm fonts:fetch` يشير إلى `tools/fetch-fonts.mjs` وهو غير موجود — أنشئه في الخطوة 3 (سكربت ينزّل woff2 لخطي IBM Plex Sans Arabic وSpace Grotesk إلى `packages/ui/fonts/` مرة واحدة، والخطوط تُحفظ في المستودع بعدها).

## 1. `packages/db` (المهمة 1.2)

- الترحيل `migrations/0001_init.sql` = **نسخة حرفية** من `docs/falak_pos_schema.sql` — انسخ الملف، لا تعد كتابته من فهمك.
- Drizzle schema بملف لكل مجموعة جداول بنفس تقسيم الملف 02 §2، مطابقاً للـ SQL (تحقق بمقارنة `information_schema` بعد الترحيل).
- سكربت `migrate`: ينفّذ الترحيلات الناقصة بالترتيب ويسجلها في `schema_migrations`. سكربت `reset`: يهدم ويعيد البناء ثم seed.
- سكربت `seed`: مستأجر "سوبرماركت الفلك" + فرع رئيسي + مخزن + صندوق كاش + طرق الدفع الثلاث (نقد/بطاقة/حساب) + ضريبة 16% شاملة + صفوف `sequences` لـ sale/return/purchase + الأدوار الخمسة من الملف 01 §5 **بصلاحيات المصفوفة §6 كاملة نصاً** + مستخدم owner (username: khaled، كلمة مرور من env) + كاشير (PIN `1234`) + وحدات القياس + 30 تصنيفاً + **500 صنف** واقعي بالعربية بباركودات EAN-13 صحيحة الـ check digit، بينها أصناف بوحدتين (حبة/كرتونة بعامل 12 أو 24) وأصناف بعدة باركودات.
- **اختبارات (vitest ضد `TEST_DATABASE_URL`):** سيناريو متوسط التكلفة من الملف 02 §4 حرفياً (شراء 10×4.00 ثم 10×6.00 ثم بيع 3 ⇒ الرصيد 17.000 والمتوسط 5.0000)؛ `next_invoice_no` يعطي BR1-YYYY-000001 ثم 000002 بلا فجوات؛ حركة `customer_transactions` تحدّث `customers.balance`؛ الحذف الناعم لا يظهر في البحث؛ و`UNIQUE (tenant_id, barcode)` يرفض التكرار.

## 2. `packages/core` + `packages/contracts` (المهمة 1.8)

- `core`: دوال نقية بلا اعتماد على القاعدة: حساب سطر، خصم سطر/فاتورة، ضريبة شاملة ومضافة، تحويل الوحدات بالـ factor، تقريب حسب `currencies.decimals` (دالة تقريب **واحدة** — ADR-002)، وتوزيع دفع متعدد العملات بسعر صرف مثبّت مع الباقي. كل الحساب بـ decimal.js.
- اختبارات شاملة تشمل: الجمع قبل التقريب عبر 100 سطر موزون (1.235 كغ × 78.50)، خصم أكبر من السطر (يُرفض)، دفع شيكل+دينار وباقٍ بالشيكل، ضريبة صفر، ودينار بثلاث منازل.
- `contracts`: zod schemas للكيانات التي تمر بين الواجهة والوكيل في هذه المرحلة فقط (مستخدم/جلسة، صنف، وحدة، باركود، سعر، تصنيف، نتيجة بحث الباركود، طلبات الاستيراد) — الأنواع تُشتق منها ولا تُكتب يدوياً في أي مكان آخر.

## 3. `packages/ui` + `packages/i18n` (المهمة 1.5)

- `tokens.css` و`tailwind.config.ts` **نسخاً حرفياً** من الملف 03 §14. تذكير: لا أخضر إطلاقاً؛ شارة `b-ok` بأيقونة ✓.
- الخطوط محلية (سكربت الخطوة 0.2)، `@font-face` مع fallback، لا Google Fonts.
- المكوّنات: Button (sm/md/xl + خاصية kbd)، Input/Field (حالة خطأ برسالة تشرح الحل)، Badge، Tag، Table (رأس surface، عمود `num` LTR، تصفّح 50 صفاً)، Dialog (Esc/Enter + نافذة PIN بست خانات)، Toast (مع زر تراجع)، Sidebar (ليلية بالتدرج §6.6)، Kpi (+hot واحدة)، EmptyState، Skeleton — كل القيم من الـ tokens، وقصص بسيطة أو صفحة معرض داخلية لمعاينتها.
- `i18n`: `ar.json` و`en.json` و`terms.supermarket.json` (وهياكل فارغة للأنواع الأخرى)، دالة `t()` تدمج مصطلحات النشاط فوق الأساس. العربية RTL افتراضية والتبديل يعيد التحميل.

## 4. `apps/agent` (المهمتان 1.3 و1.4)

- Fastify على `AGENT_PORT` (5111) + Drizzle على `DATABASE_URL` (تذكّر: المنفذ 5433 على هذا الجهاز).
- المصادقة: `POST /auth/login` (username+password بـ argon2) و`POST /auth/pin` (userId+PIN) ⇒ JWT محلي بمدة `AGENT_SESSION_TTL_HOURS`. middleware يحمّل المستخدم ودوره وصلاحياته.
- `can(permission)`: يفهم الصيغة `module.action` والحد الرقمي `module.action:10`؛ الرفض دائماً 403 برسالة عربية واضحة. **التحقق في الخادم لكل مسار** — بدّد أي شك بقراءة الملف 01 §6.
- CRUD: التصنيفات، الوحدات، الأصناف (مع وحداتها وباركوداتها وأسعارها في معاملة واحدة)، قوائم الأسعار. كل كتابة عبر مساعد `withAudit` (transaction + `audit_logs` + `sync_outbox`) — أنشئه هنا لأنه سيُستخدم في كل المراحل.
- `GET /products/barcode/:code` بالاستعلام الحرفي من الملف 02 §5.1، و`GET /products/search?q=` بالـ trigram (حرفان فأكثر).
- استيراد Excel: رفع الملف، معاينة أول 20 صفاً مع اكتشاف الأعمدة، ثم تنفيذ بتقرير لكل صف (نجح/فشل ولماذا).
- اختبارات API: دخول صحيح/خاطئ، كاشير يحاول `products.edit_price` فيُرفض 403، إنشاء صنف كامل ثم جلبه بالباركود، واستيراد ملف فيه صفوف خاطئة عمداً.

## 5. `apps/pos` (المهمتان 1.6 و1.7)

- Vite + React، التوجيه، حارس جلسة، شاشة الدخول (كلمة مرور + لوحة PIN)، الهيكل: Sidebar + Topbar حسب الملف 03 §5.1، تبديل اللغة والمظهر (يُحفظ لكل مستخدم).
- شاشات: الأصناف (قائمة ببحث فوري وتصفية وترقيم)، نموذج الصنف (وحدات متعددة، باركودات مع زر توليد داخلي، أسعار لكل قائمة مع حد أدنى، صورة)، التصنيفات، الوحدات، الاستيراد بمعاينة.
- طبّق قائمة الفحص في الملف 03 §15 على كل شاشة، واختبار Playwright واحد على الأقل: دخول بـ PIN → إنشاء صنف → البحث عنه بالباركود.

## التوازي المقترح (القاعدة 7.3)

بعد إنجاز الخطوة 1 (db) والـ contracts، شغّل بالتوازي: subagent لـ `packages/ui + i18n`، وsubagent لـ `apps/agent`، ثم `apps/pos` فوقهما. وبعد كل خطوة رئيسية subagent مراجعة على الـ diff (تكرار، مخالفة للوثائق، ثغرات، اختبارات ناقصة) وأصلح ملاحظاته قبل المتابعة.

## معيار الإنجاز — لا تعلن الانتهاء قبله كاملاً

- من جهاز نظيف: `pnpm i && pnpm db:reset && pnpm dev` يعطي نظاماً أدخل إليه بـ PIN 1234.
- استيراد ملف Excel بـ 500 صنف ينجح والبحث بالباركود يرد في أقل من 100ms (اطبع القياس في الاختبار).
- `pnpm lint && pnpm typecheck && pnpm test` خضراء كلها، وCI أخضر على الفرع.
- الشاشات تعمل عربي/إنجليزي وفاتح/داكن، ولوحة المفاتيح وحدها تكفي لإنشاء صنف.
- `CLAUDE.md` محدَّث (حالة المشروع)، وأي انحراف موثّق ADR.
- في النهاية أعطني ملخصاً: ما أُنجز، ما القرارات التي اتخذتها، ما يحتاج مراجعتي أنا.

**خارج النطاق — لا تلمسه:** الكاشير، المبيعات، المخزون، الورديات، الطباعة، Tauri، السحابة. `pnpm dev` يشغّل agent وpos فقط.
